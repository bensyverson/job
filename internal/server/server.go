// Package server will host the HTTP web view for job (`job serve`).
// Stubbed until the design lands; kept as a named package so downstream
// phases can import without churn.
package server

// New serves the job web view — stubbed.
