{{- /* Home ("Now") page. Signal cards + mini-graph ship in a later task;
     this file is the page template that mounts the shared chrome and
     a placeholder body so the route returns real HTML. */ -}}
{{define "title"}}Home · Jobs{{end}}
{{define "content"}}
<section class="stack stack-gap-md">
  <h1>Jobs</h1>
  <p class="t-muted">The dashboard is under construction. Signal cards and the live graph arrive with the Home view task.</p>
</section>
{{end}}
